<div class="jumbotron">
  <div class='container'>
    <h1>GeoPhoto</h1>
    <p>GeoPhoto allows you to upload photos tagged by location and sorted into albums.</p>
    <% if !logged_in? %>
    <%= link_to 'Sign Up', new_user_path, class: "btn btn-primary btn-lg" %>
    <%= link_to 'Sign In', login_path, class: "btn btn-primary btn-lg" %>
    <% else %>
    <p>What's up, <%= current_user.username %>? Welcome to GeoPhoto!</p>
    <%= link_to "View Albums", user_albums_path(user_id: current_user.id), class: "btn btn-primary btn-lg" %>
    </div>

    <% end %>

  </div>
</div>
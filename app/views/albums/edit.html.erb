Edit album:

<%= form_for([@user, @album]) do |f| %>
  <div>
    <%= f.text_field :title, placeholder: "Enter album title here." %>
  </div>
  <%= f.submit "Edit album!" %>
<% end %>

<%= link_to "Delete Album", album_path, method: :delete, data: { confirm: "Are you sure you want to delete this album?" } %>